spring:
  ai:
    mcp:
      client:
        enabled: true
        type: SYNC
        name: elastic-mcp-client
        version: 1.0.0
        sse:
          connections:
            elasticsearch-mcp-server:
              url: http://localhost:8080
              sse-endpoint: /mcp
